[pytest]
# 测试目录用 tests/，与存放实验产物的 test/ 目录区分开
testpaths = tests

# 默认只跑快测（不加载本地模型）；跑慢测用：pytest -m slow
addopts = -m "not slow"

markers =
    slow: 需要加载本地 embedding / rerank 模型，较慢。默认跳过，用 pytest -m slow 执行

python_files = test_*.py
