"""生成测试用的 docx 和 pdf 文档（用于验证多格式解析 + 扩充评测数据）。

扩写说明：为保证知识库文档内容充足、评测集有区分度，docx / pdf 均已扩写到
3000+ 字。改动文档内容后需重跑 ingest.py 重建索引，并重新评估评测集。
"""
from pathlib import Path

DOCS = Path(__file__).resolve().parent / "data" / "docs"


def gen_docx():
    from docx import Document

    doc = Document()
    doc.add_heading("面向对象编程", level=1)

    doc.add_heading("类与对象", level=2)
    doc.add_paragraph(
        "类是面向对象编程中最核心的概念。类是对一类具有相同属性和行为的对象的抽象模板，"
        "对象是类具体实例化出来的个体。比如「人」是一个类，它抽象了所有人共有的特征——"
        "姓名、年龄、身高这些属性，以及走路、说话、吃饭这些行为；而「张三」是「人」这个类"
        "的一个具体对象，他有具体的名字、具体的年龄。可以这样理解：类是图纸，对象是按图纸"
        "造出来的房子；图纸可以造出无数座结构相同但细节不同的房子。"
    )
    doc.add_paragraph(
        "在代码层面，类用 class 关键字定义，类里定义的变量称为属性（字段），类里定义的函数"
        "称为方法。创建对象的过程称为实例化，构造方法（如 Python 里的 __init__）在对象被创建"
        "时自动调用，用来初始化对象的属性。通过 self（Python）或 this（Java/C++）可以在方法"
        "内部访问当前对象自己的属性。对象之间的交互靠调用彼此的方法完成，调用者不需要关心"
        "方法内部是怎么实现的，这就是面向对象设计的基本思想。"
    )
    doc.add_paragraph(
        "为什么要把数据和操作数据的方法绑定在一起？核心目的有两个：一是内聚，把相关的数据和"
        "逻辑放在一起，代码结构清晰、便于维护；二是复用，一个类写好后可以在多处创建对象使用，"
        "避免重复造轮子。面向对象的三大特性——封装、继承、多态，都是在类和对象的基础上展开的。"
    )

    doc.add_heading("封装", level=2)
    doc.add_paragraph(
        "封装是把数据和对数据的操作绑定在一起，隐藏内部实现细节，只对外暴露必要的接口。"
        "它的本质是「信息隐藏」：对象内部的状态不应该被外部随意修改，外部只能通过公开的方法"
        "去访问和修改。比如一个银行账户类，余额这个属性应该被保护起来，外部不能直接给它赋值，"
        "而要通过存款、取款这样的方法来操作，方法内部可以加入校验逻辑，比如取款前检查余额是否充足。"
    )
    doc.add_paragraph(
        "实现封装的常用手段是访问控制。Python 用下划线约定：以单下划线开头的属性表示「受保护」，"
        "以双下划线开头的属性会做名称改写，提示外部不要直接访问；Java 和 C++ 则用 private、"
        "protected、public 关键字严格控制访问级别。工程实践中，一般把属性设为私有，通过公开的"
        "getter 和 setter 方法（Python 里用 @property 装饰器）提供受控的读写入口。"
    )
    doc.add_paragraph(
        "封装的好处有三点：第一，降低耦合，外部只需要依赖稳定的公开接口，内部怎么改都不影响"
        "调用方；第二，保护数据，防止非法状态被写入，比如年龄不能是负数、价格不能是负值，这类"
        "约束可以放在方法里统一校验；第三，便于维护和演进，内部实现可以从简单的变量换成复杂的"
        "计算或数据库存取，只要接口不变，调用方完全无感知。可以说封装是面向对象设计的第一条防线。"
    )

    doc.add_heading("继承", level=2)
    doc.add_paragraph(
        "继承让子类复用父类的属性和方法，实现代码复用，并在此基础上扩展或改写。比如有一个"
        "「动物」父类，定义了 eat 和 sleep 两个方法；「狗」「猫」作为子类，天然继承了这两个方法，"
        "不需要重新实现，同时可以添加自己特有的方法，比如狗可以「看家」、猫可以「捉老鼠」。"
        "继承表达的是「is-a」（是一种）的关系：狗是一种动物，所以凡是能对动物做的事情，"
        "对狗也应该适用。"
    )
    doc.add_paragraph(
        "子类可以重写（override）父类的方法，用自己更具体的实现替代父类的默认实现。比如父类"
        "动物有一个 make_sound 方法返回「动物叫」，子类狗把它重写为「汪汪叫」，子类猫重写为"
        "「喵喵叫」。Python 里用 super() 可以显式调用父类的方法，常用于「先执行父类逻辑，再补充"
        "子类逻辑」的场景。重写时要注意保持方法签名一致，否则调用方按父类接口调用时会出错。"
    )
    doc.add_paragraph(
        "继承的层次不能太深，一般建议最多两三层。过深的继承会让类之间的关系难以理解，"
        "修改父类时牵一发而动全身。现代面向对象设计更提倡「组合优先于继承」：如果只是想复用"
        "某个类的能力，优先用持有对象（组合）而不是继承类。继承适合「确实存在 is-a 关系」的"
        "场景，组合适合「只是需要用到它的功能」的场景。"
    )

    doc.add_heading("多态", level=2)
    doc.add_paragraph(
        "多态指同一个方法在不同子类里可以有不同的实现，调用方无需关心对象的具体类型，"
        "只要调用同一个方法，运行时就会根据对象的真实类型执行对应的版本。这是面向对象最强大"
        "的特性之一，也是实现「开闭原则」（对扩展开放、对修改关闭）的关键手段。"
    )
    doc.add_paragraph(
        "举个例子：一个动物园管理系统里有一个 feed(animal) 函数，接收任意动物对象并调用它的"
        "eat 方法。不管传入的是狗、猫还是鸭子，函数代码完全不用改，运行时各自执行自己的 eat"
        "实现。如果要新增一种动物，只要新增一个继承自动物的类并实现 eat 方法，feed 函数无需"
        "任何改动就能支持新动物。这就是多态带来的可扩展性。"
    )
    doc.add_paragraph(
        "Python 的鸭子类型让多态更进一步：只要对象具备所需的方法，不管它继承自哪里都可以"
        "参与多态调用——「如果它走起来像鸭子、叫起来像鸭子，那它就是鸭子」。这比强制继承"
        "更灵活，但也要求团队成员对接口约定足够默契。在 Java、C++ 等静态语言里，多态通常依赖"
        "继承或接口来实现。面试中常问的多态前提：继承（或实现接口）、方法重写、父类引用指向"
        "子类对象，三者缺一不可。"
    )

    doc.add_heading("构造与析构", level=2)
    doc.add_paragraph(
        "构造方法在对象创建时自动调用，主要职责是初始化对象的属性、建立对象运行所需的资源。"
        "Python 的构造方法是 __init__，注意它其实在对象内存已经分配之后被调用，真正的「分配」"
        "由 __new__ 负责，不过一般场景只需要重写 __init__。构造方法可以接收参数，把创建对象的"
        "必要信息传进来，比如 Person(\"张三\", 20) 直接把姓名和年龄作为构造参数。"
    )
    doc.add_paragraph(
        "析构方法在对象被销毁时调用，用来释放资源。Python 的析构方法是 __del__，但由于垃圾"
        "回收时机不确定，工程上更推荐用上下文管理器（with 语句）或显式 close 方法来管理文件、"
        "连接等资源，而不是依赖析构方法。构造和析构是对象生命周期的两端，理解它们有助于写出"
        "资源管理正确的代码。"
    )

    doc.add_heading("魔术方法与运算符重载", level=2)
    doc.add_paragraph(
        "Python 用双下划线包裹的魔术方法（dunder methods）让自定义类可以和语言内建语法无缝"
        "配合。比如 __str__ 控制 str(obj) 和 print(obj) 的输出；__len__ 让对象支持 len(obj)；"
        "__getitem__ 让对象支持 obj[key] 下标访问；__call__ 让对象可以像函数一样被调用；"
        "__eq__ 定义 == 的语义；__lt__ 定义 < 的语义，实现后对象就可以直接排序；"
        "__enter__ 和 __exit__ 让对象支持 with 语句。"
    )
    doc.add_paragraph(
        "运算符重载让自定义类型用起来和内置类型一样自然。比如定义了一个向量类 Vector，"
        "重写 __add__ 之后就可以直接用 v1 + v2 表示向量相加，重写 __repr__ 之后调试时打印"
        "对象能看到可读的内容。合理使用魔术方法能大幅提升代码的可读性，但也不要滥用——"
        "让运算符的行为符合直觉，否则反而会降低可读性。"
    )

    doc.add_heading("抽象类与接口", level=2)
    doc.add_paragraph(
        "抽象类是不能直接实例化的类，用来定义子类必须实现的方法骨架。Python 用 abc 模块的"
        "ABC 和 abstractmethod 定义抽象类：抽象方法只有声明没有实现，子类必须重写它，否则"
        "子类也无法实例化。抽象类的意义在于强制统一接口：所有子类都必须实现同样的方法，"
        "调用方就可以放心地按父类接口调用，从而支持多态。"
    )
    doc.add_paragraph(
        "接口在概念上比抽象类更纯粹——只声明「能做什么」，不提供任何实现。Java 的 interface"
        "是典型接口；Python 没有单独的 interface 关键字，一般用抽象类或鸭子类型来达到类似效果。"
        "设计上，接口应该小而专注：一个接口只描述一个能力，比如 Comparable（可比较）、"
        "Iterable（可迭代），这样类可以按需实现多个接口，组合出完整能力。"
    )

    doc.add_heading("组合与设计原则", level=2)
    doc.add_paragraph(
        "组合是「has-a」（拥有）关系：一个类持有另一个类的对象作为自己的属性。比如汽车类"
        "持有发动机对象和轮胎对象，而不是继承发动机。组合的优点是灵活、解耦：可以在运行时"
        "动态替换被组合的对象，比如给汽车换一个更省油的发动机。相比之下，继承的绑定关系在"
        "编译期就固定了，比较僵化。因此「组合优于继承」成为面向对象设计的一条重要经验法则。"
    )
    doc.add_paragraph(
        "除了组合优于继承，还有几个重要的设计原则值得记住。单一职责原则（SRP）：一个类只"
        "负责一件事，避免职责过载；开闭原则（OCP）：对扩展开放、对修改关闭，新增功能用新增"
        "类而不是修改已有类；依赖倒置原则（DIP）：依赖抽象而不依赖具体实现，这样换实现时"
        "调用方不用改。这些原则不是教条，而是指导我们在「现在简单」和「将来好扩展」之间"
        "做权衡的工具。"
    )

    doc.add_heading("面向对象与面向过程", level=2)
    doc.add_paragraph(
        "面向过程编程以「函数」为中心，把程序看成一系列操作的顺序组合，数据和处理数据的函数"
        "是分开的；面向对象编程以「对象」为中心，把数据和操作绑定在一起。面对简单的、一次性的"
        "任务，面向过程更直接；面对复杂、需要长期维护、多人协作的系统，面向对象的封装、继承、"
        "多态能让系统更清晰、更易扩展。实际工程中两者并不对立，Python 写脚本时用面向过程"
        "很自然，写大型框架时则大量使用面向对象。理解各自适用场景，比争论哪个更好更有意义。"
    )

    doc.save(DOCS / "面向对象编程.docx")
    print("已生成 面向对象编程.docx")


def gen_pdf():
    import os
    from reportlab.pdfbase import pdfmetrics
    from reportlab.pdfbase.ttfonts import TTFont
    from reportlab.lib.pagesizes import A4
    from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
    from reportlab.platypus import SimpleDocTemplate, Paragraph

    # 注册中文字体（黑体，带兜底）
    candidates = ["C:/Windows/Fonts/simhei.ttf", "C:/Windows/Fonts/msyh.ttc"]
    font_path = next((p for p in candidates if os.path.exists(p)), None)
    if not font_path:
        print("未找到中文字体，跳过 PDF 生成")
        return
    pdfmetrics.registerFont(TTFont("CN", font_path))

    title = ParagraphStyle("title", fontName="CN", fontSize=18, leading=24)
    head = ParagraphStyle("head", fontName="CN", fontSize=13, leading=20, spaceBefore=12)
    body = ParagraphStyle("body", fontName="CN", fontSize=11, leading=18)

    story = [
        Paragraph("网络基础", title),

        Paragraph("HTTP 与 HTTPS", head),
        Paragraph(
            "HTTP（超文本传输协议）是 Web 应用最基础的协议，它运行在 TCP 之上，采用请求-响应模型："
            "客户端（浏览器、App）发起请求，服务器返回响应。HTTP 的请求包含请求方法、URL、请求头和请求体，"
            "响应包含状态码、响应头和响应体。常见的请求方法有 GET（获取资源）、POST（创建资源）、"
            "PUT（整体更新）、PATCH（部分更新）、DELETE（删除资源），此外还有 HEAD、OPTIONS 等。"
            "状态码方面，2xx 表示成功，3xx 表示重定向，4xx 表示客户端错误，5xx 表示服务器错误。",
            body),
        Paragraph(
            "HTTP 最大的问题是明文传输。在网络传输的每一个环节——局域网、路由器、运营商——数据都裸露在"
            "数据包里，很容易被窃听和篡改。比如在公共 WiFi 上访问纯 HTTP 网站，攻击者可以轻松截获你的"
            "用户名密码。此外 HTTP 也不验证通信对方的身份，中间人完全可以冒充服务器。"
            "正是这些安全问题催生了 HTTPS。",
            body),
        Paragraph(
            "HTTPS 在 HTTP 和 TCP 之间加了一层 SSL/TLS 加密。它主要解决三个问题：机密性，数据经过对称加密，"
            "中间人无法读取内容；完整性，通过消息认证码校验数据没有被篡改；身份认证，通过数字证书验证"
            "服务器的真实身份。TLS 握手的大致过程是：客户端发送支持的加密套件，服务器返回证书和公钥，"
            "客户端验证证书后生成会话密钥并用服务器公钥加密发送，双方用这个会话密钥进行后续的对称加密通信。"
            "证书由受信任的 CA（证书颁发机构）签发，浏览器内置了 CA 根证书，所以能验证服务器身份。",
            body),
        Paragraph(
            "现代 Web 已经全面进入 HTTPS 时代：搜索引擎优先收录 HTTPS 站点，浏览器对纯 HTTP 站点会打上"
            "「不安全」的标记。对于需要登录的网站，HTTPS 更是基本要求，否则账号密码等于裸奔。配置 HTTPS"
            "现在也很简单，Let's Encrypt 等 CA 提供免费证书，配合 Nginx 几分钟就能部署。",
            body),

        Paragraph("TCP 三次握手", head),
        Paragraph(
            "TCP 是面向连接的可靠传输协议，通信双方在传输数据前必须先建立连接，这个建立连接的过程就是"
            "著名的三次握手。为什么叫三次握手？因为需要交换三组报文：第一次，客户端向服务器发送 SYN 报文，"
            "并携带自己的初始序列号，表示「我想和你建立连接」；第二次，服务器收到后回复 SYN+ACK 报文，"
            "确认收到客户端的 SYN，同时带上自己的初始序列号，表示「我收到你的请求了，我也准备好了」；"
            "第三次，客户端再发送 ACK 报文确认服务器的 SYN，双方随即进入已连接状态。",
            body),
        Paragraph(
            "为什么一定是三次而不是两次？核心原因是为了确认双方的收发能力都正常，并协商好初始序列号。"
            "只有客户端发了、服务器收到并回应、客户端再确认服务器能收到，双方才能确定「我的数据能发到你那，"
            "你的数据也能发到我这里」。如果只有两次握手，服务器无法确认客户端是否真的收到了自己的报文；"
            "同时，两次握手无法处理「迟到的重复连接请求」问题：一个迟到的旧 SYN 到达服务器，如果服务器"
            "直接建立连接，就会白白浪费资源。三次握手时，客户端收到迟到的确认会直接丢弃或回复 RST，"
            "连接不会误建立。",
            body),
        Paragraph(
            "与建立连接对应的是四次挥手，用于关闭连接。为什么断开要四次？因为 TCP 是全双工的，两个方向"
            "的关闭是独立的：一方发送 FIN 表示「我不再发数据了」，对方回复 ACK 确认，然后对方也发送 FIN，"
            "这一方再回复 ACK，连接才彻底关闭。过程中还可能存在半关闭状态，即一方已关闭发送、另一方仍在发送。"
            "另外，主动关闭方最后要进入 TIME_WAIT 状态等待 2MSL 时间，确保最后一个 ACK 送达，防止新连接"
            "收到旧连接的迟到报文。",
            body),
        Paragraph(
            "TCP 的可靠性还体现在其他机制上：滑动窗口实现流量控制，拥塞控制（慢启动、拥塞避免、快速重传）"
            "调节发送速率避免网络过载，序列号 + 确认重传保证数据不丢不乱。这些机制让 TCP 成为可靠传输"
            "的基石，但同时也有一定的头部开销和延迟，因此实时音视频、游戏这类对延迟敏感的场景会改用 UDP。",
            body),

        Paragraph("UDP 与 TCP 的对比", head),
        Paragraph(
            "UDP（用户数据报协议）是无连接的、不可靠的传输协议：发送方只管把数据报扔到网络上，不建立连接、"
            "不确认、不重传、不保证顺序。它没有 TCP 的握手和拥塞控制开销，延迟低、实时性好。"
            "适用场景包括：实时音视频通话（允许少量丢包）、在线游戏（延迟优先）、DNS 查询、DHCP。"
            "TCP 适合对可靠性要求高的场景：网页浏览、文件传输、邮件、数据库连接。"
            "简单说，TCP 用「慢一点」换「不丢不乱」，UDP 用「可能丢」换「快」。很多应用会结合两者："
            "比如视频通话的媒体流走 UDP，信令控制走 TCP。",
            body),

        Paragraph("Cookie 与 Session", head),
        Paragraph(
            "HTTP 是无状态协议，服务器不记得上一次请求是谁发的。为了让网站记住用户（登录状态、购物车），"
            "引入了 Cookie 和 Session 机制。Cookie 是保存在客户端浏览器的一小段数据，由服务器通过 Set-Cookie"
            "响应头下发，浏览器后续每次请求都会自动带上。Session 是保存在服务器端的数据，客户端只保存一个"
            "session id，服务器用这个 id 在内存或 Redis 中找到对应的会话数据。",
            body),
        Paragraph(
            "Cookie 和 Session 各有优缺点。Cookie 存在客户端，容量小（一般 4KB 左右）、可以被用户查看和修改、"
            "存在被 XSS 窃取的风险，但服务器无状态、好扩展；Session 存在服务端，容量大、更安全、可以主动"
            "失效，但服务器有状态，多台机器部署时必须共享 Session（通常存 Redis）。两者经常配合使用："
            "登录成功后服务器创建 Session，把 session id 写进 Cookie，之后每次请求浏览器带 Cookie，"
            "服务器查 Session 判断登录态。",
            body),
        Paragraph(
            "针对 Cookie 的安全问题，要注意几个要点：HttpOnly 属性让 JavaScript 无法读取 Cookie，"
            "能有效防止 XSS 窃取；Secure 属性让 Cookie 只在 HTTPS 连接下传输；SameSite 属性可以限制"
            "跨站携带 Cookie，防止 CSRF 攻击。除此之外，现代应用越来越多使用 Token 方案（如 JWT）："
            "把用户信息编码进签名后的 token 里，服务器验签即可，无需存储，天然适合分布式和无状态场景。"
            "登录态方案的选择取决于场景：传统 Web 用 Session，前后端分离、移动端接口用 JWT 更常见。",
            body),

        Paragraph("DNS 与域名解析", head),
        Paragraph(
            "当我们访问 www.example.com 时，浏览器首先需要知道这个域名对应的服务器 IP 地址，这个查询过程"
            "就是 DNS 解析。DNS 是一个分层的分布式系统：本地缓存 → 递归服务器 → 根服务器 → 顶级域服务器"
            "→ 权威服务器，逐级找到 IP。整个过程通常在几十毫秒内完成，大部分查询都有缓存。"
            "常见的 DNS 记录类型有 A（IPv4 地址）、AAAA（IPv6 地址）、CNAME（别名）、MX（邮件服务器）、"
            "TXT（文本记录，常用于域名验证）。DNS 是互联网的基础设施，一旦 DNS 故障，整个网站都会无法访问。",
            body),

        Paragraph("IP 地址与端口", head),
        Paragraph(
            "IP 地址用于定位网络中的一台设备：IPv4 是 32 位，如 192.168.1.1；IPv6 是 128 位，"
            "解决 IPv4 地址枯竭问题。端口号用于区分一台设备上的不同服务：一个 IP 地址加上一个端口"
            "就能唯一定位一个服务。常见端口：HTTP 是 80，HTTPS 是 443，SSH 是 22，MySQL 是 3306，"
            "Redis 是 6379。公网 IP 有限，内网设备通过 NAT 共享公网出口，所以从公网访问内网服务"
            "需要端口映射或内网穿透。",
            body),

        Paragraph("TCP/IP 分层模型", head),
        Paragraph(
            "理解网络分层是排查网络问题的基础。TCP/IP 模型分为四层：应用层（HTTP、DNS、FTP）、"
            "传输层（TCP、UDP）、网络层（IP、ICMP）、网络接口层（以太网、WiFi）。OSI 七层模型"
            "把它进一步细分，但实际通信主要遵循 TCP/IP 四层。分层的好处是每层只关心自己的职责，"
            "下层为上层提供服务。排查问题时也按层来：先看应用（错误码）、再看端口和传输（连通性）、"
            "再看网络（IP、路由）、最后看物理链路。",
            body),
    ]
    SimpleDocTemplate(str(DOCS / "网络基础.pdf"), pagesize=A4).build(story)
    print("已生成 网络基础.pdf")


if __name__ == "__main__":
    gen_docx()
    gen_pdf()
